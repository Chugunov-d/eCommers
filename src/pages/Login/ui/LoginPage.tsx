import { Link } from "react-router-dom";
const LoginPage = () => {
    return (
        <div>
            LoginPage
            <Link to={'/'}>Home</Link>           
        </div>
    );
}

export default LoginPage;
